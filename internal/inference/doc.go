// Package inference implements bounded completed-transcription, speech, cleanup,
// and metadata HTTP capabilities. One shared transport enforces redirect policy;
// response metadata parsing and aggregation retain the same privacy boundary.
package inference
