---
title: Backend compatibility
description: Choose a backend for transcription, cleanup, and text to speech.
---

import ProviderIcon from "../../../../components/ProviderIcon.astro";
import { availableBackends } from "../../../../data/backends";
export const base = import.meta.env.BASE_URL.replace(/\/$/, "");

Freehand connects to infrastructure you choose. Start with the
[visual backend directory](../../backends/) to compare operations, then use
a guide below to get connected. For model-specific recognition, cleanup, and voice controls,
visit the separate [model directory](../../models/) and [model profile guides](../models/).

:::note[Version compatibility]
Your installed release may have fewer options than these guides. Check each
guide's server version and model requirements before configuring a connection.
:::

<div class="provider-guide-grid">
  {availableBackends.map((backend) => (
    <a class="provider-guide-card" href={`${base}${backend.guide}`}>
      <ProviderIcon profile={backend.id} size={32} />
      <span>
        <strong>{backend.name}</strong>
        <small>{backend.summary}</small>
      </span>
    </a>
  ))}
  <a class="provider-guide-card" href={`${base}/docs/backends/planned/`}>
    <span>
      <strong>Planned profiles</strong>
      <small>Upcoming integrations and alternatives.</small>
    </span>
  </a>
</div>

## Run a backend

For installation inside Freehand, start with [Local runtimes](../guides/local-runtime/).
That guide lists the supported NeMo, whisper.cpp, and llama.cpp models, tasks,
and platforms, including optional MagpieTTS speech alongside NeMo transcription.
Downloads are explicit.

The launch recipes below are for services you manage separately. Use them for
other models, custom runtime settings, or a server on another machine.

| Server                    | Launch instructions                                                                                             | Freehand operation                         |
| ------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| whisper.cpp               | [Native Windows](./whisper-cpp/#run-natively-on-windows) or [Docker](./whisper-cpp/#run-whispercpp-with-docker) | Transcription with a server-loaded model   |
| vLLM                      | [Windows + Docker, NVIDIA GPU](./vllm/#run-vllm-with-docker)                                                    | Qwen3-ASR transcription or S1-mini cleanup |
| Speaches                  | [Docker GPU or CPU](./speaches/#run-speaches-with-docker)                                                       | Transcription; optional speech playback    |
| Kokoro-FastAPI            | [Docker CPU or GPU](./kokoro-fastapi/#run-kokoro-fastapi-with-docker)                                           | Speech playback with voice discovery       |
| llama.cpp                 | [Native Windows server](./llama-cpp/#run-llamacpp-on-windows)                                                   | S1-mini or compatible custom cleanup       |
| NeMo-Speech.cpp           | [Runtime installation](./nemo-speech/#run-the-server)                                                           | Completed/live transcription and MagpieTTS speech |
| vLLM-Omni                 | [Qwen3-TTS serving guide](./vllm-omni/#run-the-server)                                                          | Text to speech with preset voices          |
| Generic OpenAI-compatible | [Connect an existing endpoint](./generic/)                                                                      | The operations implemented by your server  |

These local examples use PowerShell and loopback ports. Docker recipes require
Docker Desktop with Linux containers; NVIDIA examples also require a working
WSL2 GPU setup. Follow [Docker's GPU setup](https://docs.docker.com/desktop/features/gpu/)
if `docker run --gpus ...` cannot access the card. First use downloads the image
and the selected model. Model weights stay in the recipe's folder or named
volume when a container stops.

The launch examples include stop and restart commands; guides that link to
upstream setup use that runtime's instructions. If you run several models on
one GPU, leave enough memory for each or stop an unused server first.

For a remote or hosted server, use the same profile with the administrator's
HTTPS base URL, model ID, and authentication instead. Loopback-bound examples
are for a client on the same PC. See [connection topologies](../guides/connect-a-server/#choose-a-topology)
for separate STT, cleanup, and speech endpoints.

## Check your connection

**Check connection** reads server health or model metadata. **Refresh voices**
lists voices on supported speech servers. Neither action generates audio or
runs transcription. Use a short recording or voice preview to try your selection.

## Choose profiles per operation

Voice transcription, Audio-file transcription, Cleanup, and Text to speech each
select their own connection. Tasks using the same managed runtime share its
selected model and get the matching model profile automatically.

For manual connections, manage endpoint, authentication, and backend profile in
**Connections** on the activity rail. Use each workflow's **Settings** cog to open
its right options sidebar for models, model profiles, voices, and other
model options; Freehand remembers those options for each
connection and model.

## Beyond the current contract

Transcription context and temperature depend on the selected backend and model
profile. [Vocabulary](../guides/vocabulary/) supplies recognition terms through
the supported request fields. See the [language guide](../guides/languages/)
for language selection.

Voice-style instructions are available with [Qwen3-TTS](../models/qwen3-tts/)
on vLLM-Omni. Translation, timestamps, diarization, reference-audio inputs, and
progressive speech playback are not available. A server may offer these features
without Freehand exposing them.

Use the [protocol reference](../reference/protocol/) for exact request fields
and safety limits.
