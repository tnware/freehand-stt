---
title: Backend compatibility
description: Choose a backend for transcription, cleanup, and text to speech.
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';
import ProviderIcon from "../../../../components/ProviderIcon.astro";
import { availableBackends } from "../../../../data/backends";
export const base = import.meta.env.BASE_URL.replace(/\/$/, "");

Choose a **backend profile** for the API your server exposes. Choose a
**model profile** separately for the languages and controls of its loaded model.
The server can run on your computer, a LAN server, a VPS, or a hosted service.

<CardGrid>
  <LinkCard title="Compare backends" href="../../backends/" description="Browse the visual directory by transcription, cleanup, and speech capabilities." />
  <LinkCard title="Choose model behavior" href="../models/" description="Find supported languages, recognition hints, cleanup, and voice controls." />
</CardGrid>

:::note[Version compatibility]
Your installed release may have fewer options than these guides. Check each
guide's server version and model requirements before configuring a connection.
:::

<div class="provider-guide-grid">
  {availableBackends.map((backend) => (
    <a class="provider-guide-card" href={`${base}${backend.guide}`}>
      <ProviderIcon profile={backend.id} size={32} />
      <span>
        <strong>{backend.name}</strong>
        <small>{backend.summary}</small>
      </span>
    </a>
  ))}
  <a class="provider-guide-card" href={`${base}/docs/backends/planned/`}>
    <span>
      <strong>Planned profiles</strong>
      <small>Upcoming integrations and alternatives.</small>
    </span>
  </a>
</div>

## Run a backend

:::tip[Let Freehand manage a local installation]
Start with [Local runtimes](../guides/local-runtime/) for supported NeMo,
whisper.cpp, and llama.cpp models and platforms, including optional MagpieTTS
speech alongside NeMo transcription. Downloads are explicit.
:::

Use the recipes below when you manage the service yourself, need other models
or runtime settings, or run the server on another machine.

| Server                    | Launch instructions                                                                                             | Freehand operation                         |
| ------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| whisper.cpp               | [Native Windows](./whisper-cpp/#run-natively-on-windows) or [Docker](./whisper-cpp/#run-whispercpp-with-docker) | Transcription with a server-loaded model   |
| vLLM                      | [Windows + Docker, NVIDIA GPU](./vllm/#run-vllm-with-docker)                                                    | Qwen3-ASR transcription or S1-mini cleanup |
| Speaches                  | [Docker GPU or CPU](./speaches/#run-speaches-with-docker)                                                       | Transcription; optional speech playback    |
| Kokoro-FastAPI            | [Docker CPU or GPU](./kokoro-fastapi/#run-kokoro-fastapi-with-docker)                                           | Speech playback with voice discovery       |
| llama.cpp                 | [Native Windows server](./llama-cpp/#run-llamacpp-on-windows)                                                   | S1-mini or compatible custom cleanup       |
| NeMo-Speech.cpp           | [Runtime installation](./nemo-speech/#run-the-server)                                                           | Completed/live transcription and MagpieTTS speech |
| vLLM-Omni                 | [Qwen3-TTS serving guide](./vllm-omni/#run-the-server)                                                          | Text to speech with preset voices          |
| Generic OpenAI-compatible | [Connect an existing endpoint](./generic/)                                                                      | The operations implemented by your server  |

### Before running a local recipe

- **Shell and containers:** examples use PowerShell and loopback ports. Docker
  recipes require Linux containers; NVIDIA examples need working WSL2 GPU
  access. Follow [Docker's GPU setup](https://docs.docker.com/desktop/features/gpu/)
  if `docker run --gpus ...` cannot access the card.
- **Downloads and storage:** first use downloads the image and selected model.
  Stopping a container keeps weights in its recipe's folder or named volume.
- **Resources and shutdown:** leave memory for every concurrent model or stop
  unused servers. Recipes include stop/restart commands or link upstream setup.

For a remote or hosted deployment, use the same profile with the administrator's
HTTPS base URL, model ID, and authentication. Loopback recipes serve a client on
the same computer. See [connection topologies](../guides/connect-a-server/#choose-a-topology)
for separate transcription, cleanup, and speech endpoints.

## Check your connection

| Action | What it checks |
| --- | --- |
| **Check connection** | Health or model metadata |
| **Refresh voices** | Voice metadata on supported speech servers |
| Short recording or voice preview | Runs only the model you explicitly selected so you can review its output |

Health and discovery never generate audio or run transcription.

## Choose profiles per operation

| Where | Configure |
| --- | --- |
| **Connections** on the activity rail | Endpoint, authentication, backend profile, and allowed uses |
| Workflow **Settings** cog | Active connection, model, model profile, voices, and model options |
| **Local runtimes** | Installation and loaded models for managed connections |

Voice transcription, Audio-file transcription, Cleanup, and Text to speech
select their own connection. Tasks using one managed runtime share its selected
model and get the matching profile automatically. Manual model options are
remembered per connection and model.

## Beyond the current contract

Transcription context and temperature depend on the selected backend and model
profile. [Vocabulary](../guides/vocabulary/) supplies recognition terms through
the supported request fields. See the [language guide](../guides/languages/)
for language selection.

Voice-style instructions are available with [Qwen3-TTS](../models/qwen3-tts/)
on vLLM-Omni. Translation, timestamps, diarization, reference-audio inputs, and
progressive speech playback are not available. A server may offer these features
without Freehand exposing them.

Use the [protocol reference](../reference/protocol/) for exact request fields
and safety limits.
