---
title: Planned backend profiles
description: Upcoming backend integrations and alternatives.
---

import ProviderIcon from "../../../../components/ProviderIcon.astro";
import { plannedBackends } from "../../../../data/backends";

These profiles appear disabled in Freehand. A compatible endpoint may already
work through **Generic OpenAI-compatible**. Planned describes dedicated profile
work; it is not a promise of a release date or every feature of a provider.

{plannedBackends.map((backend) => (

<section aria-labelledby={backend.guide.split("#")[1]}>
  <h2 id={backend.guide.split("#")[1]}>
    <ProviderIcon profile={backend.id} size={28} /> {backend.name}
  </h2>
  <p>{backend.summary}</p>
  <ul>
    {backend.entries.map((entry) => (
      <li>
        <strong>{entry.role}:</strong> {entry.description}
      </li>
    ))}
  </ul>
</section>
))}

## Use a compatible endpoint today

If the service offers OpenAI-compatible transcription, chat, or speech routes,
try the **Generic OpenAI-compatible** profile for that operation. Dedicated
provider features will appear when their profiles become available.
