---
title: Planned backend profiles
description: Profiles that are not available and when to use a Generic connection instead.
---

import ProviderIcon from "../../../../components/ProviderIcon.astro";
import { plannedBackends } from "../../../../data/backends";

These dedicated profiles are not available in Freehand. A compatible endpoint may already
work through **Generic OpenAI-compatible** if it accepts Freehand's request
fields and response formats. No release date is promised for these profiles.

{plannedBackends.map((backend) => (

<section aria-labelledby={backend.guide.split("#")[1]}>
  <h2 id={backend.guide.split("#")[1]}>
    <ProviderIcon profile={backend.id} size={28} /> {backend.name}
  </h2>
  <p>{backend.summary}</p>
  <ul>
    {backend.entries.map((entry) => (
      <li>
        <strong>{entry.role}:</strong> {entry.description}
      </li>
    ))}
  </ul>
</section>
))}

## Use a compatible endpoint today

Compare the service's transcription, chat, or speech API with the
[Generic profile requirements](../generic/) before connecting. Generic does not
enable provider-specific features or unrelated realtime protocols.
