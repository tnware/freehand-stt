---
title: Planned backend profiles
description: Profiles that are not available and when to use a Generic connection instead.
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';
import ProviderIcon from "../../../../components/ProviderIcon.astro";
import { plannedBackends } from "../../../../data/backends";

:::note[Not available yet]
These dedicated profiles are planned, with no promised release date. A compatible
endpoint may already work through **Generic OpenAI-compatible** when it accepts
Freehand's request fields and response formats.
:::

{plannedBackends.map((backend) => (

<section aria-labelledby={backend.guide.split("#")[1]}>
  <h2 id={backend.guide.split("#")[1]}>
    <ProviderIcon profile={backend.id} size={28} /> {backend.name}
  </h2>
  <p>{backend.summary}</p>
  <ul>
    {backend.entries.map((entry) => (
      <li>
        <strong>{entry.role}:</strong> {entry.description}
      </li>
    ))}
  </ul>
</section>
))}

## Use a compatible endpoint today

<CardGrid>
  <LinkCard title="Check the Generic contract" href="../generic/" description="Compare your service's transcription, chat, or speech API before connecting." />
</CardGrid>

Generic does not enable provider-specific features or unrelated realtime protocols.
