---
title: Planned backend profiles
description: Dedicated provider contracts still awaiting implementation and qualification.
---

import ProviderIcon from "../../../../components/ProviderIcon.astro";
import { plannedBackends } from '../../../../data/backends';

These profiles appear disabled in Freehand. A compatible endpoint may already
work through **Generic OpenAI-compatible**. Planned describes dedicated profile
work; it is not a promise of a release date or every feature of a provider.

The role list and reasons below come from the app's profile catalog. Enabling
an entry requires implemented behavior, regression fixtures, and documented
model/version qualifications. Live evidence is recorded separately from source
inspection and client-only tests.

{plannedBackends.map((backend) => (
  <section aria-labelledby={backend.guide.split('#')[1]}>
    <h2 id={backend.guide.split('#')[1]}><ProviderIcon profile={backend.id} size={28} /> {backend.name}</h2>
    <p>{backend.summary}</p>
    <ul>{backend.entries.map((entry) => <li><strong>{entry.role}:</strong> {entry.description}</li>)}</ul>
  </section>
))}

## Shared qualification work

Before enabling any dedicated profile, define the supported operation and
request/response shape, distinguish model-specific fields, cover malformed and
incomplete responses, and document applicable upload/audio limits. Preserve
metadata-only tests, credential isolation, cancellation, and no automatic
inference replay. An unimplemented advanced feature remains unavailable even
when another operation on the same provider is supported.

Contributors can use the [maintenance guide](../../development/backend-compatibility/)
for the exact update and validation process.
